import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    main: "#1760a5",
    primary: "skyblue",
  },
  secondary: {
    main: "#15c630",
  },
  otherColor: {
    main: "#999",
  },
});
